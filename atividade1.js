//1- Crie uma função que receba dois números como parâmetro, retorne a soma dos valores, em seguida execute a função mostrando na tela o seguinte texto: 'A soma dos valores é ${(resultado)}'.]

/* function somarValores(n1,n2) {
let total = n1 * n2
return total 
}

alert(`A soma dos valores é: ${somarValores(10,20)}`)
 */


/* function somarValores(n1,n2,n3) {
  let valores = (n1 + n2) / n3
  return valores
}

alert(`A soma dos valores é: ${somarValores(3,10,2)}`) */

function valoresSomados(n1,n2,n3) {
  let total = n1 * n2 + n3
  return total
}

alert(`A soma dos valores é: ${valoresSomados(2,4,10)}`)

